---
name: "记忆管理"
description: "开箱即用的本地记忆系统。首次使用时问一次知识库路径，自动检测知识库内容：有存量则扫描生成记忆草稿、用户确认后批量写入；无存量则建空目录、跟随成长。"
---

# Memory Manager

## 第一性原理

记忆的本质是让 AI 在对话中能对用户的私人信息执行 CRUD：
- **Create**：新信息被识别、分类、存入正确的位置
- **Read**：相关信息在需要时被精确唤醒
- **Update**：已有事实被更新，旧版本被安全备份
- **Delete**：过时信息被收敛归档，防止熵增

"开箱即用"意味着：**零配置启动 + 全自动路由**。

---

## 一、初始化协议（首次启动，仅一次）

### 1.1 引导初始化

**首次启动时**，AI 检测到 `basePath` 未定义，执行以下步骤：

1. **询问用户**："请告诉我你的知识库根目录路径（如 Obsidian vault 的路径），记忆系统将在此目录下创建 `0 记忆系统/` 文件夹。"
2. 用户提供路径后，将路径存储为 `basePath`。
3. **检测知识库内容**：检查 `basePath` 下是否存在非记忆系统的 `.md` 文件或其他文档。

---

### 1.2 分支判断：知识库有内容 vs 知识库为空

初始化路径在此处根据知识库状态分岔：

```
用户提供 basePath
        │
        ▼
  检测 basePath 下是否有存量文档
        │
    ┌───┴───┐
    │       │
    有      无
    │       │
    ▼       ▼
  Scenario A   Scenario B
  批量扫描     跟随成长
  存量写入     从零积累
```

---

#### Scenario A：知识库已有内容 → 批量扫描初始化

**核心逻辑**：AI 主动通读知识库，从存量文档中提取 L2/L3/L4 内容生成记忆草稿，交用户审核。

**步骤详解**：

1. **AI 提案**（不直接执行）：
   ```
   检测到你的知识库已有 {n} 个文件（{basePath}）。
   是否要我扫描这些文件，自动提取以下信息来初始化记忆系统？
   
   • L2 个人画像：从你的笔记中提取身份、角色、技术栈
   • L3 经验洞察：从你的项目中提取决策与经验
   • L4 表达风格：从你的写作中提取语气与排版偏好
   
   扫描后我会先列出清单由你审核，确认后再写入。
   ```

2. **用户确认后执行扫描**：
   - 扫描范围：`basePath` 下所有 `.md` 文件（排除 `0 记忆系统/` 自身）
   - 扫描深度：每个文件读取头部内容 + 结构轮廓，不强制全文通读（避免大库耗时）
   - 提取策略：

   | 扫描到的内容特征 | 映射到记忆层级 |
   |------------------|--------------|
   | "我是 / 我在做 / 我的专业" 等身份线索 | L2 个人画像 → 基本信息.md |
   | 项目复盘、方案对比、决策理由 | L3 经验洞察 → 项目-{主题}.md |
   | 学习笔记、认知模型、读书笔记 | L3 经验洞察 → 学习-{领域}.md |
   | 个人日志、生活反思、投资记录 | L3 经验洞察 → 生活-{主题}.md |
   | Markdown 排版、语气、句式特征 | L4 表达风格 → core-style.md |

3. **生成审核清单**（格式示例）：

   ```
   === 记忆初始化审核清单 ===
   
   📋 L2 个人画像（共发现 X 条）
   □ 角色：内容创作者 / 产品经理
   □ 技术栈：Python, Obsidian, Remotion
   □ 长期目标：建立个人知识体系
   
   📋 L3 经验洞察（共发现 X 条）
   □ 项目-周然ZhouRan.md（2 条项目决策）
   □ 学习-产品思维.md（3 条认知框架）
   □ 生活-投资记录.md（1 条投资反思）
   
   📋 L4 表达风格（共发现 X 条）
   □ core-style.md（2 条语气偏好）
   
   以上内容是我从现有文件中提取的初步印象。
   请勾选你确认要写入的条目（全部/部分/修改后写入），
   或者告诉我需要调整的地方。
   
   指令示例：
   - "全部写入" → 批量执行
   - "L3 的第三条不要" → 排除特定条目
   - "帮我改一下 L2 的角色描述" → 修改后写入
   ```

4. **用户确认后批量写入**：
   - 创建 `0 记忆系统/` 目录及 L1-L4 子目录
   - 按确认后的清单一次性写入所有文件
   - **首次批量写入不触发备份协议**（此时尚无历史版本需保护）
   - 写入完成后报告：

   ```
   [记忆系统初始化完成]
   • 已创建 {basePath}/0 记忆系统/
   • 已写入 L2 个人画像：{n} 条
   • 已写入 L3 经验洞察：{n} 条
   • 已写入 L4 表达风格：{n} 条
   
   之后在对话中我会自动读取和更新这些记忆。
   你可以随时说"状态"查看当前记忆，说"记一下"来保存新信息。
   ```

---

#### Scenario B：知识库为空 → 跟随成长

**核心逻辑**：建空目录和种子文件，后续在对话中逐步积累。

1. **自动创建目录结构**（静默执行）：

```
{basePath}/0 记忆系统/
├── L1 会话/
├── L2 个人画像/
├── L3 经验洞察/
├── L4 表达风格/
└── 备份存档/
```

2. **创建种子文件**（静默执行）：

`L1 会话/当前工作上下文.md`：
```markdown
# 当前工作上下文
> 更新时间：{首次启动日期}

## 1. 当前核心焦点
- （空）

## 2. 活跃项目线程
- （空）

## 3. 下一步待办
- （空）
```

`L2 个人画像/基本信息.md`：
```markdown
---
name: ""
role: ""
tech_stack: []
relationships: []
---

# 个人信息
（空）
```

`L4 表达风格/core-style.md`：
```markdown
# 核心表达风格
> 通过增量学习逐步建立，初始为空。
```

`备份存档/.gitkeep`（空标记文件）

3. **向用户报告**：

```
[记忆系统已就绪]
路径：{basePath}/0 记忆系统/

你的知识库目前是空的，记忆系统会跟随你的对话逐步成长。
你可以说"状态"查看当前记忆，说"记一下"来保存信息。
```

---

### 1.3 重启检测

**后续对话启动时**，AI 检测到 `basePath` 已存在，直接进入 Read Loop（见协议一），不再重复初始化流程。

### 1.4 目录自愈

若 AI 在写入时发现目标文件或父目录不存在（用户误删等），**静默重建**空文件/目录，不打断用户。

---

## 二、架构：四层认知模型

```
记忆系统根目录 ({basePath}/0 记忆系统/)
│
├── L1 会话/         ← 工作记忆（当前在忙什么）
├── L2 个人画像/     ← 静态事实（你是谁）
├── L3 经验洞察/     ← 长期记忆（你学到了什么）
└── L4 表达风格/     ← 表达偏好（你怎么说）
```

| 层级 | 名称 | 定义 | 写入方式 | 备份 |
|------|------|------|----------|------|
| **L1** | 会话 | 当前焦点任务、活跃项目、下一步待办 | 全量覆写 | 不备份 |
| **L2** | 个人画像 | 身份、角色、技术栈、人际关系等结构化属性 | 属性覆写 + 增量追加 | 写前备份 |
| **L3** | 经验洞察 | 项目决策、生活反思、认知框架等非结构化经验 | 增量追加 | 写前备份 |
| **L4** | 表达风格 | 语气偏好、排版规范、句式习惯 | 增量追加 | 写前备份 |

### MECE 写入边界（L2 vs L3 的核心区分）

这是系统中最重要的分类规则，AI 必须严格执行：

**属于 L2 个人画像的条件**（含以下信号词 → L2）：
- "我是 / 我在 / 我有 / 我的…是" 等身份定义
- 技术栈、工具链、职业角色
- 人际关系、家庭结构
- 长期目标、人生信条（静态版本）
- 身体特征、性格标签

**属于 L3 经验洞察的条件**（含以下信号词 → L3）：
- "我决定 / 我选择了" 等决策陈述
- "我学到 / 我发现 / 我意识到" 等学习结论
- "复盘 / 总结 / 反思" 等回溯性内容
- 对某个项目/产品/事件的具体分析
- 认知框架的推导过程

**属于 L4 表达风格的条件**（含以下信号词 → L4）：
- "我喜欢简洁的 / 我习惯用" 等表达偏好
- "不要用 / 少用 / 避免" 等否定式风格约束
- 排版、格式、语气相关的具体要求

**判断优先级**：L4 > L2 > L3（风格偏好的信号最强，其次是身份声明，最后才是经验）

**模糊边界的处理**：若一条内容同时匹配 L2 和 L3 的特征（如 "我是一个相信长期主义的产品经理，所以我决定不做短期的流量项目"），拆分为两条分别存入：身份部分 → L2，决策部分 → L3。若无法拆分，**显式询问用户**。

---

## 三、操作协议

### 协议一：主动检索与唤醒（Read Loop）

#### 3.1.1 会话开启（自动读取）
每次新对话开始时，自动读取：
- `L1 会话/当前工作上下文.md`
- `L2 个人画像/基本信息.md`

#### 3.1.2 经验动态唤醒（按需读取）

**触发条件**：用户提及特定项目名、人名、领域词，且上下文属于以下类型之一：
- 制定计划、征求建议
- 回溯决策、复盘
- 深入讨论某话题

**不读取的情况**（去噪）：
- 纯反问（"XX 是什么来着？"）
- 否定句（"今天不聊 XX"）
- 无意义提及、开玩笑

**检索粒度**（按优先级从高到低）：
1. **文件名前缀匹配**：用户提到"周然"→ 扫描 `L3 经验洞察/` 下含"周然"的文件名
2. **标签匹配**：文件内有 `#项目决策` 等标签，且与当前话题匹配
3. **全文关键词**：前两步无结果时，扫描文件头几行的关键词

**读取量控制**：单次最多读取 3 个最相关的 L3 文件，避免上下文过载。

#### 3.1.3 输出前风格校对（自动读取）
在输出长文章、正式回应、总结报告前，自动读取 `L4 表达风格/core-style.md`。

---

### 协议二：状态查询（Status）

用户发送 "状态" / "记忆状态" 时：

1. 读取 L1 当前焦点任务
2. 读取 L2 画像中用户的长期目标
3. 扫描 L3 文件最近修改时间，列出最近更新的 3 个主题
4. 以简洁格式呈报：

```markdown
📋 记忆系统状态

**当前焦点**：{L1 内容摘要}
**画像概览**：{L2 核心属性摘要}
**最近更新**：{L3 最近修改的文件列表}
```

---

### 协议三：保存个人画像（Save Profile - L2）

**触发信号**：
- 显式："记一下 / 记住 / 保存"
- 隐式：对话中自然流露的身份声明，AI 可主动提案（见 Guardrails）

**分类与路由规则**：

| 内容类型 | 目标文件 | 说明 |
|----------|----------|------|
| 基本信息、角色、技术栈 | `L2 个人画像/基本信息.md` | 核心画像文件 |
| 人际关系、家庭 | `L2 个人画像/人际关系.md` | 首次出现时自动创建 |
| 长期目标、人生信条 | `L2 个人画像/目标与信条.md` | 首次出现时自动创建 |
| 其他无法归类的画像信息 | `L2 个人画像/{主题}.md` | 按内容主题自动命名创建 |

**写入方式**：
- YAML Frontmatter 区域（`基本信息.md`）：键值对**覆写**
- 非属性区域：**增量追加**，不覆写已有内容

---

### 协议四：保存经验洞察（Save Insight - L3）

**触发信号**：
- 显式："记一下 / 保存 / 记住这个决策"
- 伴随触发：用户做出明确决策或得出清晰结论时，AI 可主动提案

**文件命名规则**（自动路由）：

```
{类型}-{主题}.md
```

| 内容主题 | 文件名 | 示例 |
|----------|--------|------|
| 项目/产品决策 | `项目-{项目名}.md` | `项目-周然ZhouRan.md` |
| 生活/心态/投资 | `生活-{主题}.md` | `生活-日记洞察.md` |
| 学习/认知框架 | `学习-{领域}.md` | `学习-产品思维.md` |
| 工作/流程改进 | `工作-{主题}.md` | `工作-自动化流程.md` |
| 书籍/课程 | `书籍-{书名}.md` | `书籍-时间价值手册.md` |
| 其他 | `通用-{YYYY-MM}.md` | 按月汇总 |

**文件名匹配逻辑**（AI 判断流程）：
1. 从内容中提取核心主题词
2. 检查 `L3 经验洞察/` 下是否有匹配的已有文件（文件名包含主题词）
3. 有 → 追加到已有文件
4. 无 → 按上表规则创建新文件

**写入格式**：

```markdown
## YYYY-MM-DD：标题
**类型**：[项目/生活/学习/工作/书籍] | **标签**：#标签1 #标签2

核心内容及背景推导，保持上下文完整。
```

**标签规范**（可选，用于跨文件检索）：
- `#项目决策` `#认知模型` `#流程改进` `#产品思考` `#投资` `#写作方法` 等

---

### 协议五：学习表达风格（Learn Style - L4）

**触发信号**：
- 用户主动："学习一下我的风格"
- 用户提供原文 vs 改稿的对比
- AI 观察到用户反复表达同一偏好时主动提案

**写入目标**（按场景分流）：

| 场景 | 目标文件 |
|------|----------|
| 通用语气、句式、排版偏好 | `L4 表达风格/core-style.md` |
| 长文章格式 | `L4 表达风格/format-article.md` |
| 书籍/笔记格式 | `L4 表达风格/format-book.md` |
| 短视频脚本格式 | `L4 表达风格/format-short-video.md` |

**写入方式**：增量追加，不整文件重写。

**长期风格 vs 单篇特例的区分**：
- 用户在多篇内容中**一致**表现的偏好 → 写入全局风格文件
- 用户仅在某篇指定的格式要求 → 记为例外说明，不写入全局

---

## 四、触发词映射

| 用户说 | 执行操作 |
|--------|----------|
| "状态" / "记忆状态" | Status（协议二） |
| "记一下" / "记住" / "保存" | Save Profile 或 Save Insight（根据内容分类） |
| "这个是我 / 我是…" 身份声明 | Save Profile — L2 |
| "我决定 / 我的结论是" 决策声明 | Save Insight — L3 |
| "学习" / "学一下我的风格" | Learn Style（协议五） |
| "更新记忆" | Save Insight + Status |

---

## 五、安全写入与备份协议

### 5.1 备份范围

| 层级 | 写前备份？ | 原因 |
|------|-----------|------|
| L1 会话 | **不备份** | 频繁覆写，避免备份目录冗余 |
| L2 个人画像 | **备份** | 事实型数据，误写损失大 |
| L3 经验洞察 | **备份** | 新增时备份（追加已有文件），新建时不需备份 |
| L4 表达风格 | **备份** | 追加时备份 |

### 5.2 备份操作流程

修改 L2/L3/L4 已有文件前：

1. 复制原文件到 `{basePath}/0 记忆系统/备份存档/`
2. 命名格式：`{原文件名}_{YYYYMMDD_HHmmss}.bak.md`
3. 执行写入
4. 回报用户：写入路径 + 备份路径

### 5.3 备份保留策略

每个源文件保留最近 **10 个** 备份文件。当第 11 个备份产生时，自动删除该文件最早的那个备份。

---

## 六、代谢收敛机制（防熵增）

### 触发条件
**每次写入 L3 文件时**，检查目标文件：
- 决策记录是否超过 **5 条**
- 文件总字数是否超过 **2000 字**

满足任一条件 → 执行收敛。

### 收敛规则

1. **仅移入归档的条件**（以下内容移至文件底部 `## 历史更迭归档`）：
   - 被后续决策明确覆盖的记录
   - 用户明确表示"不再适用"的记录
   - 时间超过 6 个月且未被 AI 读取过的早期记录（基于写入时间判断）

2. **保留在主干的条件**：
   - 最新的核心认知/方法论
   - 最近 3 次的决策记录
   - 不确定保留价值的历史记录（不出现在主干也不移入归档，保持原位）

3. **收敛格式**：

```markdown
# {文件名}

## 当前核心认知
（经过提炼的最新认知，保持简洁）

## 近期记录
### YYYY-MM-DD：……
### YYYY-MM-DD：……

## 历史更迭归档
（被覆盖或明确废弃的旧记录，移至此处保留完整性）
```

---

## 七、输出合约

每次写入操作完成后，AI 返回以下格式：

```markdown
[记忆更新]
• 位置：{文件相对路径}
• 操作：{新增 / 更新 / 覆写}
• 内容：{一句话摘要}
• 备份：{备份文件路径}（仅 L2/L3/L4）
```

---

## 八、守护规则（Guardrails）

### 8.1 写入权限
- **显式触发**（用户说"记一下"）：直接执行写入，写完后报告
- **伴随触发**（AI 判断值得记）：**必须先提案询问**，用户确认后再写入
- **静默不写**：日常寒暄、情绪发泄、纯疑问句 → 不写入

### 8.2 提案话术规范

当 AI 判断某内容值得记录时：

```
我注意到你提到了 {内容摘要}，这条信息看起来属于 {L2/L3/L4}，
是否要记录到 {目标文件}？
```

用户确认 → 执行写入。用户拒绝或忽略 → 静默丢弃，不追问。

### 8.3 不脑补原则
仅记录用户明确表达的事实与决策。不编造、不推演、不自作主张补充细节。

### 8.4 变更透明
每次写入后按输出合约回报，让用户始终知道记忆系统发生了什么变化。
